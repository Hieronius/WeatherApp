import Foundation

struct CoordinateService {
    static func isValid(latitude: Double, longitude: Double) -> Bool {
        // Add validation logic here (e.g., check latitude and longitude ranges)
        return true
    }
}
